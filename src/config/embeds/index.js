/**
 * Embeds predeterminados del bot 00Y4n.
 *
 * Cómo agregar uno:
 * 1. Publicá el embed en Discord (Sapphire u otro).
 * 2. /exportar-embed mensaje_id:<id> formato:js
 * 3. Pegá el código generado en un archivo nuevo acá, ej: carmeet.js
 * 4. Exportalo desde este index si querés usarlo en comandos/paneles.
 *
 * Ejemplo de uso:
 *   import { carmeetPrincipal } from '../../config/embeds/index.js';
 *   await channel.send({ embeds: [carmeetPrincipal] });
 */

// import { embed as carmeetPrincipal } from './carmeet.js';
// export { carmeetPrincipal };

export default {};
